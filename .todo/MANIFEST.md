# Todo Manifest — vivarium-workbench

Index of plans under `.todo/plans/`. One line per plan.
`.todo/` is gitignored — these are user-local, not part of the repo history.

| Plan                                                             | Status | Summary |
|------------------------------------------------------------------|---|---|
| 1. [composites-tab-dispatch](plans/1-composites-tab-dispatch.md) | FINALIZED (gap-audit 2026-07-21, pending Alex go-ahead) | Composition-native sim for ANY pbg-template workspace (v2ecoli=#1): Composites tab characterizes (native introspection), studies compose composites (native `default_state_ref`/nesting), platform runs any doc on smscdk (compose-on-Batch; new `ComposeSimulationServiceRay` + registry-extension, replacing the SP-D remote-exec stub). Fixes the 404 + closes a verified allowlist/IAM gap. |