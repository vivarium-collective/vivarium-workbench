# Perspective — 2026-07-17

## Executive summary

The v0.3.0 release is genuinely done and deployed — `sms-api`'s `sms-api-stanford`
overlay pins `vivarium-workbench` to `newTag: 0.3.0` (confirmed live in the
kustomization, resolving the "unconfirmed" flag in both `NEXT_STEPS.md` and
`SAVE_SLOT.md`). The blocking work is finished. What's left is entirely the demo:
record the 8-segment `WALKTHROUGH.md` GovCloud walkthrough. Two things sit between
"ready" and "recorded": (1) the pinned-build gate is stale by four `v2ecoli` main
commits, one of which is thematically loaded (`v2ecoli` PR #339 just renamed its
own stale `vivarium-dashboard` references to `vivarium-workbench` — the rename
this repo has been carrying as a deprecated-alias shim is now complete upstream
too), and (2) five demo-prep files landed on `demo-verification` with no owning
plan until today's commit (`09975c5`) retroactively logged them as Plan 11 — they
introduce a *second* demo flow ("local dashboard, remote compute" /
`WALKTHROUGH-local-remote-compute.md` + full narration script) that has never been
rehearsed or reconciled against the primary `WALKTHROUGH.md`. Nobody has yet said
out loud which flow is being recorded, or what changes to `WALKTHROUGH.md` are
needed before it is — that's the open question this perspective ends on.

---

## 1. Where the four repos actually stand (branch activity)

### vivarium-workbench (this repo)
- `main` is at `a98cb8c` (PR #474, import-linter gate) — three refactor PRs
  (#472 staging-policy-split, #473 ScientificContent port, #474 import-linter
  gate) landed on `main` *after* the `v0.3.0` release cut, none related to the
  demo. `demo-verification` has merged all of them in (`3b4dd60`) and adds one
  doc-only commit (`09975c5`) on top.
- Working tree has one uncommitted, deliberately-unbundled change:
  `pyproject.toml`/`uv.lock` repointing the `pbg-ptools` path dependency from
  `../pbg-ptools` (doesn't exist locally) to `../../sms/pbg-ptools` (does) —
  a real local-env fix, intentionally kept out of the demo-prep commit per
  Plan 10's scope note. Still sitting uncommitted.
- Stale/orphaned branches worth noting but not urgent: `demo` (old, PR #452),
  `demo-v2ecoli` (upstream gone — this was the *previous* demo branch name,
  superseded by `demo-verification`), `feat/serve-api-base-url` (upstream gone).

### sms-cdk
- `main` at `17f4033` (2026-07-14) — PR #24 `workbench-alb-cotenant` just
  landed: it adds the `vivarium-workbench` target group and `/workbench`,
  `/bigraph-loom` routes to the internal ALB, and renames
  `ptools-proxy.sh` → `sms-proxy.sh` (keeping the old name as a symlink). This
  is the infra the whole remote demo depends on — it's freshly merged, so the
  routing WALKTHROUGH.md assumes has not been live for long.
- Before that: an `add-rds-dump-script` / `dump-rds-verify-flag` pair and a
  `ray-v2ecoli-workload` PR (Batch-based Ray MNP support for v2ecoli) — the
  compute path Segment 6 Part B narrates.

### sms-api
- `main` at `7ef6570a` (2026-07-14, PR #175 `deploy/stanford-prod-workbench`)
  — adds vivarium-workbench to stanford **prod** (parity with stanford-test).
  Sitting just above it on the `deploy/workbench-0.3.0` branch: `e9862a1`
  bumps the workbench pin 0.2.0 → 0.3.0, and `2de193f` cuts release `0.9.22`
  ("workbench 0.3.0 integration"). **This is exactly the overlay confirmation
  `SAVE_SLOT.md` flagged as unchecked** — verified live in this session: the
  `sms-api-stanford` kustomization reads `vivarium-workbench newTag: 0.3.0`.
  The release tail is done.
- A `dependabot` PR (`be151d1`, pip bump) is queued on top — routine, not
  demo-relevant.
- The branch list here is enormous (~90 branches) — almost entirely `sms-api`'s
  own iteration history, not signal for the demo. The one relevant thread is
  the `deploy/workbench-0.3.0` → `main` line above.

### v2ecoli
- `main` at `a08e20bd` (2026-07-11, PR #339 `chore/rename-dashboard-to-workbench`)
  — v2ecoli's own code no longer references `vivarium-dashboard` anywhere; it
  also dropped its `vivarium-workbench` dependency entirely in the PR just
  before (#338, `5d83d16` — "viewer moved to pbg-ptools"). **This matters for
  the demo**: `WALKTHROUGH.md`'s pinned-build reference throughout
  (`main @ 70b5ec3`) is now **4 commits stale** relative to `origin/main`.
  Per the demo's own "non-negotiable constraint" (§1.1), `ensure_latest_main_build.sh`
  must be re-run before recording — this hasn't happened since 2026-07-14.
- Also notable: a `hybrid-quantum-surrogate` branch (PennyLane variational
  layer) and an in-progress `colony-surrogate-composite` checkout at a sibling
  worktree — exploratory, not demo-blocking, but shows the workspace is not
  standing still while the demo waits.

---

## 2. The demo goal — what "done" actually requires

Three converging facts, cross-checked against `NEXT_STEPS.md`, `SAVE_SLOT.md`,
`.todo/MANIFEST.md`, and the `demos/v2ecoli/` tree itself (`SAVE_SLOT.md`/
`NEXT_STEPS.md` treated as authoritative over `.todo/*` per this skill's
constraint — no conflict was actually found; `.todo/plans/11-*` and the
`NEXT_STEPS.md` note describe the same untracked-files situation consistently):

1. **The release is shipped and deployed.** `v0.3.0` is tagged, released, and
   live on `sms-api-stanford`'s workbench pod (confirmed above). This was the
   one open question left in `SAVE_SLOT.md` ("sms-api overlay pin... final
   live cutover... UNCONFIRMED") — it's resolved, not by anything in this
   repo, but by reading `~/sms/sms-api` directly as `SAVE_SLOT.md` itself
   instructed.
2. **`WALKTHROUGH.md`'s primary flow (8-segment, fully in-cluster) is
   live-verified as of 2026-07-14** per `VERIFICATION_REPORT.md` — 7½/8
   segments pass, one soft-fail (PTools Omics Launch, deferred to Plan 9).
   That verification predates sms-cdk's `workbench-alb-cotenant` merge
   (`17f4033`, same day) and now the v2ecoli main drift above — it needs a
   fresh pass, not necessarily a full re-verify, before trusting the numbers
   in a recording.
3. **A second, unreconciled demo flow exists.** The five files landed on
   `demo-verification` (`WALKTHROUGH-local-remote-compute.md`,
   `scripts/remote_commit_run.py`, `speaker/NARRATION.md`,
   `speaker/three_layers.png`, plus the pre-existing `speaker/SPEAKER_SCRIPT.md`)
   describe a **different** presenter flow than the one `WALKTHROUGH.md`
   documents:
   - `SPEAKER_SCRIPT.md` (older, not touched by Plan 11) narrates the
     **fully-local** flow — `vivarium-dashboard serve --workspace
     ~/vivarium-app/v2ecoli --port 8771`, `ptools-proxy.sh -s smsvpctest`,
     `verify_demo.py` — a flow `WALKTHROUGH.md` itself says was retired
     ("old offline seeding/verification scripts... were retired").
   - `NARRATION.md` (new, Plan 11) narrates the **primary remote** flow
     (`smscdk`, `sms-proxy.sh -s smscdk`, port 8080) — consistent with
     `WALKTHROUGH.md` — but reframes the pitch from generic
     "process-bigraph workspaces" language to **sms-api / sms-ecoli /
     "v1 and v2 simulators (vEcoli and v2ecoli)"** specific language not
     present in `WALKTHROUGH.md`'s narration cues.
   - `WALKTHROUGH-local-remote-compute.md` (new, Plan 11) documents a
     **third** variant — local dashboard serve, remote sms-api compute
     ("Path B") — explicitly *not* the presenter demo and *not* fully-offline
     dev, a middle path `NEXT_STEPS.md` references but `WALKTHROUGH.md`'s
     README doesn't mention at all in its file-layout table.

   Three narrations, three sets of assumptions, one recording to make. Nobody
   has stated which is canonical for the actual session, and `SPEAKER_SCRIPT.md`
   is flatly out of date (references retired scripts and the old
   `vivarium-dashboard` CLI name).

**Bottom line on "all hands on deck":** the blocking engineering work (release,
deploy, core walkthrough verification) is done. What's unresolved is entirely
presentation logistics — which script, which numbers, whether the pinned build
needs refreshing first — not code.

---

## 3. Concrete, actionable gaps (ranked)

1. **Stale pinned build.** `v2ecoli` main has moved 4 commits past the
   `70b5ec3` reference cited throughout `WALKTHROUGH.md`/`NARRATION.md`/
   `VERIFICATION_REPORT.md`. Run `./demos/v2ecoli/scripts/ensure_latest_main_build.sh`
   against `smscdk` before any recording — the demo's own README calls this
   non-negotiable and the last run predates the drift.
2. **Three narration documents, no stated canonical choice.** `SPEAKER_SCRIPT.md`
   is dead (references retired local flow + old CLI name) but still sits in
   `speaker/` next to the current `NARRATION.md` with no deprecation marker —
   easy to grab the wrong one live. Needs either deletion or an explicit
   "superseded by NARRATION.md" header.
3. **`WALKTHROUGH.md`'s own file-layout table (README.md:98-109) omits
   `WALKTHROUGH-local-remote-compute.md`, `speaker/NARRATION.md`,
   `speaker/SPEAKER_SCRIPT.md`, and `scripts/remote_commit_run.py`** — all
   real, current files not listed. Whatever the recording decision, the
   README's map of the directory should match what's actually there.
4. **`VERIFICATION_REPORT.md`'s 2026-07-14 numbers predate `sms-cdk`'s
   `workbench-alb-cotenant` merge** (same day, order unclear) and are now 3
   days old against a moving `v2ecoli` main — the routing path the demo
   depends on (`/workbench` + `/bigraph-loom` on the internal ALB) is recent
   enough that a quick smoke-check (not a full re-verify) before recording
   would be cheap insurance.
5. **Uncommitted `pyproject.toml`/`uv.lock` change** sits on `demo-verification`
   indefinitely (deliberately, per Plan 10) — fine as-is, but worth surfacing
   since a stray `git add -A` at any point during demo-prep commits would
   silently bundle an unrelated env fix into a demo-doc commit.

---

## 4. The open question — WALKTHROUGH.md adjustments

This is the one thing this perspective *cannot* determine from the repo alone:
you've mentioned there are adjustments needed to `demos/v2ecoli/WALKTHROUGH.md`
and its overall flow that haven't been described yet. Given what's on disk —
three competing narration docs, a routing layer that just landed, a stale
pinned build, and a rename (`vivarium-dashboard` → `vivarium-workbench`) that
v2ecoli's own repo just finished completing on its side — there's no shortage
of candidate reasons, but guessing would just be noise. Surfaced as an open
question at the end of this file rather than invented here.

---

## Perspective Δ (Cognitive Analysis)

```yaml
baseline: true
note: >
  No prior .perspective.md existed in this repo (first invocation of this
  skill here) — this is the baseline perspective. No delta analysis to run;
  future invocations should diff against this one.
salience_score: null
deltas: []
```
